<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="atzios.greenhouse.cultivations" >

    <uses-feature android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/field_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.LauncherActivity" android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name" android:screenOrientation="portrait">
        </activity>
        <activity android:name=".activities.NewGreenhouseActivity"
            android:label="@string/new_greenhouse" android:screenOrientation="portrait" />
        <activity android:name=".activities.SetupActivity" android:screenOrientation="portrait"/>
        <activity android:name=".activities.JobsAndCultActivity" android:label="@string/jobs_cult_types" android:screenOrientation="portrait"/>
        <activity android:name=".activities.NewCultivationActivity" android:label="@string/new_cultivation" android:screenOrientation="portrait" />
        <activity android:name=".activities.NewWorkActivity" android:label="@string/new_work" android:screenOrientation="portrait" />

    </application>

</manifest>
